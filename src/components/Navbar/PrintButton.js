import { Button } from "@chakra-ui/react";

const PrintButton = () => {
  return (
    <Button bg="white" _hover={{ bg: "gray.50" }}>
      Print
    </Button>
  );
};

export default PrintButton;
